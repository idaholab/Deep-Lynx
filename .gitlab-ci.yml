
deploy_development:
  stage: deploy
  image: mcr.microsoft.com/azure-cli:latest
  script:
    - az cloud set --name AzureUSGovernment
    - az login --identity
    - az aks install-cli
  environment:
    name: development

  