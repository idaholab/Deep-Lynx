
deploy_development:
  stage: deploy
  image: mcr.microsoft.com/azure-cli:latest
  script:
    - mkdir -p $HOME/.kube
    - echo -n $KUBE_CONFIG | base64 -d > $HOME/.kube/config
    - kubectl config view
  environment:
    name: development

  