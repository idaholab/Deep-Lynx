defmodule DatumWeb.ExplorerTabBase do
  @moduledoc """
  These are the common operations that all explorer tabs should have access to.
  They allow reuse without having to rewrite each coresponding component on change.
  """
end
