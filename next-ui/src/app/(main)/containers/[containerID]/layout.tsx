import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "@/app/globals.css";
import Wireframe from "@/app/components/wireframe";

import { cookies } from "next/headers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
    title: "Create Next App",
    description: "Generated by create next app",
};

// THIS LAYOUT SHOULD CONTAIN THE SIDEBAR FOR ALL PAGES WITH A CONTAINER SELECTED
// THIS LAYOUT IS NESTED WITHIN THE CONTAINERS NAVBAR LAYOUT

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    const prefersDark = cookies().get("x-theme")?.value === "dark";
    return (
        <html lang="en">
            <body>
                <Wireframe>{children}</Wireframe>
            </body>
        </html>
    );
}
